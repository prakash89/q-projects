<div class="panel panel-default media p-5">
  <a class="media-left" href="#" style="width:25%;">
    <%= display_image(project, "logo.image.url",
                      width: "125px",
                      place_holder: {
                        width: 125,
                        height: 90,
                        text: "<No Image>"}
                      ) %>
 	</a>
  <div class="media-body">
    <%= theme_item_title(project.name, admin_project_path(project)) %>
    <%= theme_item_sub_title(project.client.name) if project.client %>
 		<%= theme_item_description(project.description, 120) %>
  </div>

</div>


